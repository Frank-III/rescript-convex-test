// AUTO-GENERATED by convex-rescript-codegen
// DO NOT EDIT - Generated from convex/rooms.ts
// Run `bun run codegen` to regenerate

open ConvexTypes

module Mutation_create = {
  type input = {
  name: string,
  description: option<string>,
  @as("type") type_: string,
  userId: usersId,
  }
  type t = input => promise<roomsId>
  
  @module("convex/react")
  external useMutation: 'api => t = "useMutation"
  
  let use = () => {
    useMutation(api["rooms"]["create"])
  }
}

module Query_list = {
  type input = {
  userId: option<usersId>,
  }
  type output = array<roomsDoc>
  
  @module("convex/react")
  external useQuery: ('api, input) => option<output> = "useQuery"
  
  let use = (~userId) => {
    useQuery(api["rooms"]["list"], {
      userId: userId
    })
  }
}

module Query_getById = {
  type input = {
  roomId: roomsId,
  }
  type output = option<roomsDoc>
  
  @module("convex/react")
  external useQuery: ('api, input) => option<output> = "useQuery"
  
  let use = (~roomId) => {
    useQuery(api["rooms"]["getById"], {
      roomId: roomId
    })
  }
}

module Mutation_join = {
  type input = {
  roomId: roomsId,
  userId: usersId,
  }
  type t = input => promise<roomsId>
  
  @module("convex/react")
  external useMutation: 'api => t = "useMutation"
  
  let use = () => {
    useMutation(api["rooms"]["join"])
  }
}

module Mutation_leave = {
  type input = {
  roomId: roomsId,
  userId: usersId,
  }
  type t = input => promise<JSON.t>
  
  @module("convex/react")
  external useMutation: 'api => t = "useMutation"
  
  let use = () => {
    useMutation(api["rooms"]["leave"])
  }
}

module Query_getMembers = {
  type input = {
  roomId: roomsId,
  }
  type output = array<roomsDoc>
  
  @module("convex/react")
  external useQuery: ('api, input) => option<output> = "useQuery"
  
  let use = (~roomId) => {
    useQuery(api["rooms"]["getMembers"], {
      roomId: roomId
    })
  }
}
